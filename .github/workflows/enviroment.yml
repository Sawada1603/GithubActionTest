name: enviroment
on: [push]
jobs:
  enviroment-print:
    runs-on: ubuntu-latest
    env:
        ACTOR_NAME: ${{ github.actor }}
    steps:
      - name: Print actor name
        run: echo "ACTOR_NAME: ${ACTOR_NAME}"
      
      
